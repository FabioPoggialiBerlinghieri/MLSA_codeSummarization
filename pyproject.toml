[project]
name = "mlsa-codesummarization"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "FabioPoggialiBerlinghieri", email = "fabio.poggiali@edu.unifi.it" }
]
requires-python = ">=3.11"
dependencies = [
    "datasets>=5.0.1",
    "torch>=2.14.0",
]
